name: "AutomataLibCpp"
description: "FFI bindings for automata C++ library."
output: "lib/plugin/ffi_plugin/automata_lib_cpp_bindings.dart"

headers:
  entry-points:
    - "lib/native_libs/automata_lib/automata_lib.h"

  include-directives:
    - "lib/native_libs/automata_lib/automata_lib.h"

comments:
  style: any
  length: full
